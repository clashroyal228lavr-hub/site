{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "chrome",
            "request": "launch",
            "name": "Запустити сайт (index.html)",
            "file": "${workspaceFolder}/index.html"
        }
    ]
}